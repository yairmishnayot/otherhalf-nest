import { ResponseFormatInterceptor } from './response-format.interceptor';

describe('ResponseFormatInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseFormatInterceptor()).toBeDefined();
  });
});
